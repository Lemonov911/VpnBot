aiogram==3.13.0
aiosqlite==0.20.0
python-dotenv==1.0.0
aiohttp==3.9.5
qrcode[pil]==7.4.2
Pillow==10.4.0
# pillow-heif: HEIC/HEIF support от iPhone-юзеров. Требует libheif1 на VPS
# (apt install libheif1 libheif-dev — обычно уже стоит на Ubuntu 24+).
# Если падает на установке: закомментируй, бот стартует без HEIC.
pillow-heif==0.18.0

# Test dependencies
pytest>=8.0
pytest-asyncio>=0.23
pytest-aiohttp>=1.0
