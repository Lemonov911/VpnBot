[mypy]
ignore_missing_imports = True

# handlers/ not type-checked: aiogram 3.x types from_user/bot/message as
# Optional everywhere, but the framework guarantees they're set via routing.
# This produces ~50 false union-attr per file. skip = treat as untyped stubs.
[mypy-handlers.*]
follow_imports = skip
