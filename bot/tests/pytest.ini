[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = .
markers =
    legacy_broken: pre-existing test failures, фиксят отдельно (см. tests/conftest.py:LEGACY_BROKEN_TESTS)
addopts = -m "not legacy_broken"
